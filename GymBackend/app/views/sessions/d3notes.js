var body = document.body

var r = canvas.width /4;
var 11= g.point(-50, r),
12 = g.point(canvas.width+50, r);

anim
.drawLine({ 'on': lines, 'from':lines})

var afterStep = function() {
  if (++current <steps.length)

}

"draw line": function (target, duration, options, onComplete) {
  
}
function arcTween(transition, arc, newAngle) {
  transition.attrTween("d", function(d){

  })
}
function TilingPatter(id, parent, width, height) {
  pattern = defs.append("pattern")
  .attr("id", id)
  .attr("parent width", width)
}

var svg = d3.select('svg').node(),
pattern = new TilingPattern("dodecatile", svg)

function plusN(n) {
  return function(x){return x+n}
}

function identity(x){return x;}
identity;
identity.longName = "I am the identity of the function - I return the exact thing I am given"

color.domain(d3.keys(data[0]).filter(function(key) {return key!=="State";} ));
data.forEach(function(d){
  var y0 = 0;
  d.ages = color.domain().map(function(name){

  })
})
d3.csv("data.csv", function(error, data){ourData=data;});

d3.keys(ourData[0]);
dr.keys(ourData[0]).filter(function(key){
  return key !== "State";
});
color.domain(d3.keys(data[0]).filter(function(key) {return key !== "State";}));
data.forEach(function(d){

})

//Higher Order Function:

//Passing functions to other functions...

svg.selectAll(".bar")
.data(data)
.enter().append("rect")

//Javascript map returns a new array with the results of the startArray

var startArray = [1,2,3,4,5];
var addOne = startArray.map(function(x) {return x+1;});
addOne;
var createObj= startArray.map(function(x){ return {"value":x}; });
createObjPlusTen= startArray.map(function(x) return{return {"value": })
var evenArray = startArray.map(function(x) {return x%2===0;});

var evenArray = startArray.map(function(x) {return x%2!==0;});

//If true keep it in the new array else throw it out =FILTER 
var allEvenArray= startArray.filter(function(x) {return x%2 ===0;})
//If you are using a for loop in D3 you're doing it wrong!
//JavaScript Filter Function

//reduce is a function that keeps a running total of each element and make an array of returns FINAL total of all calculations fibonacci strategy1,

[10, 20, 30, 40, 50].reduce( function ( runningTotal, currentValue, index, array){
  console.log("runningTotal:" runningTotal.toString());
console.log()
})
console.log("runningTotal:" runningTotal.toString());
console.log()

ourData.forEach(function(d){
  var y0=0;
  d.ages= color.domain().map(function(name) {return {name: name, y0: y0, y1:y0+= +d[name]};});
  d.total = d.ages[d.ages.length-1].y1;
});
var state= svg.selectAll(".state")
.data(data)
.enter().append("g")
.attr("width", width+margin.left)
state.selectAll("rect")

color.domain(d3.keys(data[0]).filter(function(key) {return key !== "State"))
