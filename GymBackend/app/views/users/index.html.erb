<% provide(:title, "All users") %>
<h1> All Users</h1>

<%= will_paginate %>
<ul class="users">
<% @users.each {|user| %>
  <li><%= gravatar_for user, size: 50 %> <%= link_to user.username, user %> </li>
<% } %>
</ul>